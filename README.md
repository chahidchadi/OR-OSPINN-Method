# OR-OSPINN-Method
OR--OSPINN is a domain decomposition PINN method for the Euler–Bernoulli beam equation. It reformulates the fourth-order PDE into a second-order complex system and couples subdomain PINNs via optimized Schwarz transmission conditions, using local and nonlocal operator approximations.
